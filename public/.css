body { padding: 10rem; font-family: sans-serif; }
    form { margin-bottom: 2rem; max-width: 600px; }
    form input, form select, form button { 
      display: block; 
      width: 100%; 
      margin: 1rem 0; 
      padding: 1rem; 
      border: var(--border); 
      border-radius: 10px 0 30px 0; 
    }
    table { width: 100%; border-collapse: collapse; margin-top: 3rem; }
    th, td { border: 1px solid #ccc; padding: 1.5rem; text-align: center; }
    img.thumb { width: 100px; height: 100px; object-fit: cover; }
    .delete-btn, .update-btn {
      margin-top: 1rem;
      display: inline-block;
      padding: 10px 5px;
      font-size: 1.7rem;
      color: #fff;
      cursor: pointer;
    }
    .delete-btn { background: #ff0000; }
    .update-btn { background: var(--main-color); }
    .delete-btn:hover, .update-btn:hover {
      border-radius: 10px 0 30px 0;
    }
    label { font-family:'Poppins'; font-size: 1.5rem; }
    button{ background: var(--main-color); transition: 0.6s; }
    button:hover { background: var(--hover-black); }